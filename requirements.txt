streamlit
pyyaml
numpy
torch
pillow
rasterio
scipy
matplotlib
gdown
transformers>=4.49
qwen-vl-utils
# qwen-vl-utils imports torchvision but does not declare it, so without this line
# `from qwen_vl_utils import process_vision_info` raises ModuleNotFoundError and the
# live Qwen path fails before it ever reaches the accelerator check.
torchvision
accelerate
asf_search
hyp3_sdk
